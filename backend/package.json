{
  "name": "taskedin-be",
  "version": "1.0.0",
  "description": "Taskedin Backend API",
  "scripts": {
    "start:dev": "dotnet watch run --project taskedin-be.csproj -- --urls https://localhost:3001;http://localhost:8080",
    "start:debug": "dotnet watch run --project taskedin-be.csproj -- --urls https://localhost:3001;http://localhost:8080",
    "setup:https": "powershell -ExecutionPolicy Bypass -File scripts/setup-https.ps1"
  }
}
